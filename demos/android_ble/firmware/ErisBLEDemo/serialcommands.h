#ifndef SERIALCOMMANDS_H
#define SERIALCOMMANDS_H

// Universal command set (INFO, S_F, S_ON, S_OFF, DESC, S_TIME, ...) comes from
// eriscommon; serialcommands.cpp adds the demo's own buttons.
#include <modules/serialcom.h>

#endif
