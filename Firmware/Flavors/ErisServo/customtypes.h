#ifndef CUSTOMTYPES_H
#define CUSTOMTYPES_H

#include <customtypes.h>  // eriscommon: floatSample_t, AnalogSample<N>

// ErisServo has no flavor-specific sample types — sinewave uses floatSample_t
// from eriscommon, and there are no analog/imu/etc. buffers in this flavor.

#endif
